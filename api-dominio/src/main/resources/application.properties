spring.application.name=api-dominio

# Server Port (A nuvem define a porta automaticamente, com fallback para 8080)
server.port=${PORT:8080}

# Conexão com o PostgreSQL no Supabase
# Substitua [SEU-ID-DO-PROJETO] pelo ID do seu projeto obtido no painel do Supabase
spring.datasource.url=jdbc:postgresql://db.xmzfeqgdfvtbnhvkqcdt.supabase.co:5432/postgres
spring.datasource.username=postgres
spring.datasource.password=B@tera929248

# Configurações do WhatsApp API
whatsapp.api.url=http://localhost:8081
whatsapp.api.key=929248
whatsapp.api.instance=dominio
# WhatsApp que receberá alertas do sistema (estoque baixo, etc.)
oficina.whatsapp.gerente=5524992147851

# Configurações do Hibernate / JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Habilita o agendador de tarefas (@Scheduled)
spring.task.scheduling.pool.size=2